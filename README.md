# Generic Animations With Variables In Protocol Extensions
A tiny animation library for generic UIView top, bottom, left, right and fade animations utilizing objc-runtime
to add properties into UIView extension

For example usage see UIViewController.swift implementation
